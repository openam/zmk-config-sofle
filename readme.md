# ZMK Config Sofle

I've been using the [keymap editor](https://nickcoutsos.github.io/keymap-editor/) to make the changes to the keymap. I've tried using close to the default layout, with the exception of the base layer being in Dvorak. See the [ZMK documentation](https://zmk.dev/docs/) to understand how the keymap works.

I'm probably going to switch my the lower layer to have the 10 key number pad like I used to have on my old Sofle.
